import { useColorModeValue } from '@chakra-ui/react';

export const flowStyle: any = {
  borderWidth: 1,
  borderRadius: 10,
  minHeight: "50vh",
  background: useColorModeValue('white', '#111c44')
}
